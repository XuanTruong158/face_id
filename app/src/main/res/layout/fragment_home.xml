<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CARD: LỚP HỌC SẮP TỚI (nhấn để mở ClassDetail) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardUpcoming"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="18dp"
        app:strokeWidth="2dp"
        app:strokeColor="@color/orange_700"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Bạn có thể giữ nội dung tối giản hoặc giống bản Home cũ -->
        <LinearLayout
            android:padding="14dp"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            <TextView
                android:id="@+id/tvBadge"
                android:text="LỚP HỌC SẮP TỚI"
                android:textStyle="bold"
                android:textSize="12sp"
                android:textColor="@color/orange_700"
                android:background="@drawable/bg_badge_orange"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
            <TextView
                android:id="@+id/tvUpcomingTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Android Hè 2025 (CSE441_01)"
                android:textStyle="bold"
                android:textSize="16sp"/>
            <TextView
                android:id="@+id/tvLecturerUp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Kiều Tuấn Dũng"
                android:textColor="@color/gray_700"/>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- HEADER: LỚP HỌC CỦA TÔI -->
    <LinearLayout
        android:id="@+id/sectionHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="8dp"
        android:gravity="center_vertical"
        app:layout_constraintTop_toBottomOf="@id/cardUpcoming"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="LỚP HỌC CỦA TÔI"
            android:textStyle="bold"
            android:textColor="@android:color/black"/>

        <TextView
            android:id="@+id/tvSeeAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Xem tất cả"
            android:textColor="@color/blue_700"/>
    </LinearLayout>

    <!-- DANH SÁCH LỚP -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvClasses"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
    app:layout_constraintTop_toBottomOf="@id/sectionHeader"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    tools:listitem="@layout/item_class_sv"/>
</androidx.constraintlayout.widget.ConstraintLayout>
